refer: [hello-agents/docs/chapter1/第一章 初识智能体.md at main · datawhalechina/hello-agents](https://github.com/datawhalechina/hello-agents/blob/main/docs/chapter1/%E7%AC%AC%E4%B8%80%E7%AB%A0%20%E5%88%9D%E8%AF%86%E6%99%BA%E8%83%BD%E4%BD%93.md)

# What
- 四个基本要素
  - 任何能够通过**传感器（Sensors）** 感知其所处**环境（Environment）**，并**自主**地通过**执行器（Actuators）**采取**行动（Action）**以达成特定目标的实体

# 智能体多维度分类
- 基于内部决策架构的分类
	- 基于模型的反射智能体
	  - 回答：世界现在是什么样子的？，拥有了初级的记忆，决策不再仅仅依赖于瞬时感知
	- 基于目标的智能体
	  - 我应该做什么才能达成目标
	- 基于效用的智能体
	  - 哪种行为能为我带来最满意的结果？
	- 学习型智能体
	  - 不依赖预设，而是通过与环境的互动自主学习
	  - 代表：强化学习
	  - 一个学习型智能体包含一个性能元件（即我们前面讨论的各类智能体）和一个学习元件。学习元件通过观察性能元件在环境中的行动所带来的结果来不断修正性能元件的决策策略。
- 基于时间与反应性的分类
	- 关注智能体是在接收到信息后立即行动，还是会经过深思熟虑的规划再行动
	- 反应式智能体
		- 近乎即时的反应，几乎不进行未来规划  eg.安全气囊
	- 规划式智能体
		- 会进行复杂的思考和规划 eg.下围棋
	- 混合式智能体
		- 经典架构--分层设计：底层是一个快速的反应模块，处理紧急情况和基本动作；高层则是一个审慎的规划模块，负责制定长远目标    eg.现代llm   思考阶段是规划，行动与观察阶段则是反应
- 基于知识表示的分类
	- 亚符号主义AI    黑箱
		- 是从海量数据中学习到的统计模式    eg.神经网络和深度学习   
		- 在看过成千上万张猫的图片后，大脑中的神经网络能辨识出“猫”这个概念的视觉模式
	- 符号主义AI
		- 传统，遵循严格的逻辑规则
	- 神经符号主义AI
		- 上述二者的结合
![智能体示意图](attachments/Pasted%20image%2020260912224446.png)


# 智能体循环
> 感知-思考-行动-观察![感知-思考-行动-观察循环](attachments/Pasted%20image%2020260912224452.png)

- perception action
  - 需要明确的交互协议规范智能体与环境之间的信息交换，即对智能体的输出进行结构化定义，包含两部分
    - Thought(思考)
    - Action

# 代码中不懂的点

## 1. re.serach()
1. ` match = re.search(r'(Thought:.*?Action:.*?)(?=\n\s*(?:Thought:|Action:|Observation:)|\Z)', llm_output, re.DOTALL)`
可能生成多轮Thougt-Action，只保留第一轮
**三部分：**
1.捕获组 `(Thought:.*?Action:.*?)`
匹配字面量字符串 "Thought:" “Action:”
`.* 贪婪匹配`
`.*?`: 非贪婪匹配。它会尽可能少地匹配字符，直到遇到下一个定义的标识符。
match.group(1)就是Thought:到Action:的内容
如果一次输出中有多个 Thought/Action 对，它只抓取当前这一组，而不会一下抓到文章末尾
假设 LLM 输出的内容是：
> Thought: 我想吃炸鸡。 Action: 订购[麦当劳] Observation: 订单已确认。

执行 `match.group(1)` 后，得到的结果是： `"Thought: 我想吃炸鸡。\nAction: 订购[麦当劳]"`
它是包含了头（Thought:）也包含了尾（Action: 及其参数）的完整块。

2.正向肯定预查(?=.......)
非捕获性断言，它检查后面是否跟着某些内容，但**不把这些内容包含在匹配结果中**。
`\n\s*`: 匹配一个换行符以及随后可能存在的任何空格
`(?:Thought:|Action:|Observation:)`: 这是一个非捕获组，匹配三种情况之一
它像是一个“软停止”信号。它告诉正则：“看到下一个标签时就停下，但别把人家的标签也给‘吃’进我的匹配结果里。”
`\Z`: 匹配字符串的绝对末尾

3.`re.DOTALL`: 这是一个关键标志。默认情况下，`.`（点号）不能匹配换行符。开启此标志后，`.` 可以匹配包括换行符在内的所有字符，从而允许正则跨行搜索整个推理块。
 默认情况（没有 `re.DOTALL`）
想象 `.` 是一双眼睛，它能看到一行里的所有东西，但一遇到回车（换行）它就瞎了。
- 文本：`Thought: I will search.\nAction: Search`
- 正则：`Thought:.*Action`
- 结果：匹配失败。因为 `.*` 走到 `search.` 之后遇到了 `\n`，它跨不过去。
 开启 `re.DOTALL` 后
这时候点号 `.` 获得了“透视眼”，它现在能看到包括换行符在内的所有字符。
- 结果：匹配成功。它会把 `\n` 当成普通字符直接跨过去，从 `Thought:` 一直抓取到 `Action:`。
2. ` re.search(r"(\w+)\(", action_str).group(1)`
（\w+）：
（）表示捕获组，它告诉正则：“把匹配到的这个单词存起来，我待会儿要用（通过 `group(1)` 取出）
\w代表”单词字符“，包括字母、数字、下划线_
+表示一个或多个`\w+` 会匹配一个完整的单词
`\(`：匹配左括号 `(` 本身
它在寻找：**一个后面紧跟着左圆括号的单词**
**没反斜杠的括号代表捕获组，是去抓内容的**
## 2.strip()
去掉字符串首尾的空白字符，只去前后，不去中间
包括空格 换行\n 制表符 \t 回车\r

## 运行截图
![智能体相关图片](attachments/Pasted%20image%2020260912201005.png)
![](Pasted%20image%2020260912224257.png)
# 题目
1. 请分析以下四个 `case` 中的**主体**是否属于智能体，如果是，那么属于哪种类型的智能体（可以从多个分类维度进行分析），并说明理由：
    `case A`：**一台符合冯·诺依曼结构的超级计算机**，拥有高达每秒 2EFlop 的峰值算力
    `case B`：**特斯拉自动驾驶系统**在高速公路上行驶时，突然检测到前方有障碍物，需要在毫秒级做出刹车或变道决策
    `case C`：**AlphaGo**在与人类棋手对弈时，需要评估当前局面并规划未来数十步的最优策略
    `case D`：**ChatGPT 扮演的智能客服**在处理用户投诉时，需要查询订单信息、分析问题原因、提供解决方案并安抚用户情绪
内部决策架构角度：
case A:基于模型
case B:学习型
case C:学习型
case D: 学习型
基于时间与反应性角度：
case A：反应式
case B:混合式
case C:规划式
case D: 混合式
基于知识表示的分类角度：
case A:符号主义
case B:神经符号主义
case C:神经符号主义
case D: 亚符号主义

1. 假设你需要为一个"智能健身教练"设计任务环境。这个智能体能够：
    - 通过可穿戴设备监测用户的心率、运动强度等生理数据
    - 根据用户的健身目标（减脂/增肌/提升耐力）动态调整训练计划
    - 在用户运动过程中提供实时语音指导和动作纠正
    - 评估训练效果并给出饮食建议
    请使用 PEAS 模型完整描述这个智能体的任务环境，并分析该环境具有哪些特性（如部分可观察、随机性、动态性等）。
具有部分可观察性、随机性和较强的动态性

| performance | 训练计划的合理性、饮食建议的合理性、以及指导后的动作是否标准、指导是否及时   |
| ----------- | --------------------------------------- |
| environment | 用户的心率、血氧、睡眠、历史运动疲劳是否恢复以及健身房的环境比如有烟味或者过热 |
| actuators   | 语音指导、训练计划和饮食计划的调整、动作纠正                  |
| sensors     | 语音播报传感器，检测心率等数据的可穿戴设备                   |
3.
方案A对于7天之内的商品规则划分非常明确，7天以上商品全由人工审批可能人工量大
方案B全智能体决策可能会出现决策错误的情况
方案C：不再划分7天及七天以上商品，金额 `< 100RMB` 由智能体自动通过；`100-500RMB` 由智能体审核；`>500RMB` 需主管审批；而特殊商品（如定制品）一律拒绝退款
4.记忆功能可以添加一个字典，将让llm将记忆总结并存入该字典中，然后和prompt一起给llm
自动推荐备选方案需要一个门票搜索工具，推荐备选方案和被连续三次拒绝反思调整策略感觉可以写到system prompt中
5.
备课助手：可以根据课本版本和章节要求及具体其他要求生成给出详细的教案、互动
系统1主要负责快速生成和联想，例如根据教材内容快速推荐课堂活动、互动问题、案例和教案初稿；系统2则负责对生成结果进行深入分析和验证，例如检查教学目标是否符合教材要求、教学内容是否存在知识错误、课堂活动是否符合时间和学生认知水平等。
6.
- 训练数据中可能有错误，比如混入了ai生成的数据
- 没有次数限制可能会无限调用api消耗token，实测demo代码去掉最大次数限制3次就返回了